// Link: https://coddy.tech/challenges/natural_ingredients_concatenation_js

function combineIngredients(ingredients, separator = ", ") {
    // Concatenate all ingredients using the specified or default separator
    return ingredients.join(separator);
}
