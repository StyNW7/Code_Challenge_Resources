// Link: https://coddy.tech/challenges/ocean_navigation_js

function navigateOcean(currentPosition, destination) {
    return currentPosition[0] === destination[0] || currentPosition[1] === destination[1] ? true : false
}
