// Link: https://coddy.tech/challenges/wilderness_signpost_message_js

function createSignpostMessage(distance, landmark) {
    // Construct the message using template literals
    const message = `Follow the trail for ${distance} miles to the ${landmark}.`;
    return message;
}
